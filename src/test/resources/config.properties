# Default values. Any key can be overridden on the command line, e.g. ./mvnw test -Dbrowser=firefox
baseUrl=https://the-internet.herokuapp.com
browser=chrome
headless=false
# Explicit wait timeout, in seconds
timeout=10
